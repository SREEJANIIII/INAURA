# INAURA frontend env
# Copy to .env for local dev and fill with real values. Never commit .env
# Supabase — public anon key only (service_role must NEVER be exposed here)
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
VITE_API_URL=http://localhost:8000/api/v1
